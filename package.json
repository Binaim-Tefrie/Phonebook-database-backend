{
  "name": "p3",
  "version": "1.0.0",
  "description": "Backend for the phonebook - part3",
  "main": "yes",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../p2 && npm install && npm run build && cp -r dist ../p3",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Biniam Tefrie",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
